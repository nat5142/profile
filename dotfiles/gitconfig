[user]
	name = Toby DiPasquale
	email = toby@cbcg.net
[alias]
	co = checkout
	ci = commit
	st = status
	pu = push
	pl = pull
	pub = "!f() { git push ${1:-origin} `git symbolic-ref HEAD`; }; f"
	rl = reflog
	df = diff
	dfc = diff --cached
[web]
	browser = open
[color]
	branch = auto
	diff = auto
	status = auto
	interactive = auto
[color "branch"]
	current = yellow reverse
	local = yellow 
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan

